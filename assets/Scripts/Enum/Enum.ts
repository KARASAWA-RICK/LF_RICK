export enum VAL_TYPE_ENUM {
  STR = "STR",
  NUM = "NUM",
}

//本地存储数据的键
//必须都是string
export enum SAVE_KEY_ENUM {
  SCORE = "SCORE",
}

//事件
export enum EVENT_ENUM {
  PAUSE = "PAUSE",
  RESUME = "RESUME",
  INIT = "INIT",
  RANK = "RANK",
}

//音效
export enum SOUND_ENUM {
  WIN = "WIN",
  LOSE = "LOSE",
}

//弹窗
export enum PROP_ENUM {
  VIEW = "VIEW",
  AD = "AD",
}
